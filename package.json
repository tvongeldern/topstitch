{
  "name": "root",
  "private": true,
  "devDependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^6.8.0",
    "lerna": "^3.20.2"
  },
  "scripts": {
    "dev": "docker-compose -f dev/docker-compose.yml up",
    "build": "docker-compose build",
    "start": "docker-compose up",
    "down": "docker-compose down",
    "reboot": "docker-compose down && docker-compose up"
  }
}
