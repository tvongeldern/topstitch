import { db } from '@db';
import { id } from './_fields';

export const {{jsClassName name}} = db.define('{{schemaName name}}', {
	id: {
		type: DataTypes.UUID,
		primaryKey: true,
		allowNull: false,
		defaultValue: Sequelize.UUIDV4,
	},
});
